{% set version = '1.11.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-proc
  version: {{ version|replace("-", "_") }}

source:
  fn: pROC_{{ version }}.tar.gz
  url:
    - {{ cran_mirror }}/src/contrib/pROC_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/pROC/pROC_{{ version }}.tar.gz
  sha256: 5f7e6a23a327a7c0d3effb07acc08649d1af25e8d03fc5f8b92b489f44f9c1ec

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: microbenchmark, tcltk, MASS, logcondens, doParallel, testthat, vdiffr
requirements:
  build:
    - {{ compiler('c') }}        # [not win]
    - {{ compiler('cxx') }}      # [not win]
    - {{native}}toolchain        # [win]
    - {{posix}}filesystem        # [win]
    - {{posix}}make

  host:
    - r-base
    - r-rcpp >=0.11.1
    - r-ggplot2
    - r-plyr

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-rcpp >=0.11.1
    - r-ggplot2
    - r-plyr

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('pROC')"           # [not win]
    - "\"%R%\" -e \"library('pROC')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: http://expasy.org/tools/pROC/
  license: GPL (>= 3)
  summary: Tools for visualizing, smoothing and comparing receiver operating characteristic (ROC
    curves). (Partial) area under the curve (AUC) can be compared with statistical tests
    based on U-statistics or bootstrap. Confidence intervals can be computed for (p)AUC
    or ROC curves.
  license_family: GPL3

extra:
  recipe-maintainers:
    - johanneskoester
    - bgruening
    - mingwandroid

# The original CRAN metadata for this package was:

# Package: pROC
# Type: Package
# Title: Display and Analyze ROC Curves
# Version: 1.11.0
# Date: 2018-03-24
# Encoding: UTF-8
# Depends: R (>= 2.14)
# Imports: plyr, utils, methods, Rcpp (>= 0.11.1), ggplot2,
# Suggests: microbenchmark, tcltk, MASS, logcondens, doParallel, testthat, vdiffr
# LinkingTo: Rcpp
# Authors@R: c(person("Xavier", "Robin", role = c("cre", "aut"),  email = "pROC-cran@xavier.robin.name"), person("Natacha", "Turck", role = "aut"), person("Alexandre", "Hainard", role = "aut"), person("Natalia", "Tiberti", role = "aut"), person("Frederique", "Lisacek", role = "aut"), person("Jean-Charles", "Sanchez", role = "aut"), person("Markus", "Muller", role = "aut"), person("Stefan", "Siegert", role = "ctb",  comment = "Fast DeLong code",  email = "stefan_siegert@gmx.de"))
# Description: Tools for visualizing, smoothing and comparing receiver operating characteristic (ROC curves). (Partial) area under the curve (AUC) can be compared with statistical tests based on U-statistics or bootstrap. Confidence intervals can be computed for (p)AUC or ROC curves.
# License: GPL (>= 3)
# URL: http://expasy.org/tools/pROC/
# LazyLoad: yes
# NeedsCompilation: yes
# Packaged: 2018-03-24 17:49:48 UTC; xavier
# Author: Xavier Robin [cre, aut], Natacha Turck [aut], Alexandre Hainard [aut], Natalia Tiberti [aut], Frederique Lisacek [aut], Jean-Charles Sanchez [aut], Markus Muller [aut], Stefan Siegert [ctb] (Fast DeLong code)
# Maintainer: Xavier Robin <pROC-cran@xavier.robin.name>
# Repository: CRAN
# Date/Publication: 2018-03-25 14:53:33 UTC

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
